import React from 'react';
import {Grid} from '@material-ui/core';

const GroupIcon = () => (
  <span
    style={{
      position: 'relative',
      height: '75px',
      width: '75px',
      background: '#C4C4C4',
      margin: '20px',
      borderRadius: '50%',
    }}
  />
);

export default GroupIcon;
