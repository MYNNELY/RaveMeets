import styled from 'styled-components';

export const StyledDescription = styled.div`
    line-height: 1.5em;
    overflow: hidden;
    maxHeight: 300px;

    &::-webkit-scrollbar {
        display: none;
    }
`;
